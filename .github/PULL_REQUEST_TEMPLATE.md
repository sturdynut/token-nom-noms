## Run

- Runtime and model:
- Seed (benchmark seeds are 1 to 5):
- Outcome (ticks survived, cause of death if any):

## What the agent did

One or two sentences on its token strategy. What did it compile into a reflex, what did
it prompt itself about, and what did it get wrong?

## Checklist

- [ ] `python3 -m nomnom validate` passes
- [ ] `python3 -m nomnom leaderboard` was run and `runs/README.md` is included
- [ ] The run folder is unedited
